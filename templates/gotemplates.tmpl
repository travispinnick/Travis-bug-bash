
{{define "eps.url" -}}
{{.entityId}}-{{.locale}}.html
{{- end}}


{{define "eps.source" -}}
<html>
    <body>
        <p>Hello, November 13 demo!</p>

        <h1> Hey everyone - this is a demo!  </h1>

        <h1>{{.name}}</h1>
        <p>{{.description}}</p>
        <p>Color: {{.color}}</p>
        <p>Size: {{.size}}</p>
    </body>
</html>
{{end}}

