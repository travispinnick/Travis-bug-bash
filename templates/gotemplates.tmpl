
{{define "eps.url" -}}
{{.entityId}}-{{.locale}}.html
{{- end}}


{{define "eps.source" -}}
<html>
    <body>
        <p>Hello, Feb 8 demo - happy to be here!</p>

        <h1> Hey everyone - this is a demo!  </h1>

        <h1>{{.name}}</h1>
        <p>{{.description}}</p>
        <p>Color: {{.color}}</p>
        <p>Size: {{.size}}</p>
    </body>
</html>
{{end}}

