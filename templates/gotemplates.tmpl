
{{define "eps.url" -}}
{{.entityId}}-{{.locale}}.html
{{- end}}


{{define "eps.source" -}}
<html>
    <body>
       

        <h1> Hello Brian! </h1>

        <h1>{{.name}}</h1>
        <p>{{.description}}</p>
        <p>Color: {{.color}}</p>
        <p>Size: {{.size}}</p>
    </body>
</html>
{{end}}

